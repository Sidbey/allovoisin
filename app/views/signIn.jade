extends  layout

block content
    if error
        - for (k in error)
        h2= error[k]

    form(action='/sign-in' method='post')
        p
            | Email:
            br
            input.roundedinput(name='email', value=form.email, type='text', placeholder='Email', required='')
        p
            | Password:
            br
            input.roundedinput(name='password', type='password', placeholder='Mot de passe', required='')
        input(type='submit')
    br
    br
    a(href='/sign-up') Pas encore inscrit?